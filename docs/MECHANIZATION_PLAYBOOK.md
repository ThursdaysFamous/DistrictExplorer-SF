# Mechanization Playbook — completed and archived

All three conversions are DONE (2026-07-13) and this work order moved to
`docs/archive/MECHANIZATION_PLAYBOOK.md` per its own self-archival clause.
This stub exists only so the many comment references to this path keep
resolving. The living documentation is where the machinery runs:
`docs/ENGINE_SYNC.md`, the workflows, `scripts/generate_metro_files.py`,
and `scripts/fleet_status.py`.
