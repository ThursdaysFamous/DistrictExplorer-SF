# Metro Expansion Playbook — pointer

The **master playbook lives in the Chicago repo** (the reference
implementation), at
[`DistrictExplorer-CHI/docs/METRO_EXPANSION_PLAYBOOK.md`](https://github.com/ThursdaysFamous/DistrictExplorer-CHI/blob/main/docs/METRO_EXPANSION_PLAYBOOK.md).

This fork deliberately carries no copy: full copies drift — the file that
used to live here was the verbatim Chicago master, frozen at fork time and
already missing the sections added after SF launched (§3.1 day-one fork
registration, §3.2 localization sweep — both written *because of* this
fork's port). Section references in this repo's comments and docs refer to
the master.

SF's own port record lives in this repo at
[`docs/METRO_EXPANSION_SF_WORKSHEET.md`](METRO_EXPANSION_SF_WORKSHEET.md).
Fold new lessons from any port back into the master, in the Chicago repo,
per `docs/ENGINE_SYNC.md` (port the diff, not the prompt).
