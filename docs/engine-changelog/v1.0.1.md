Script-asset release — the engine bundle is **byte-identical to v1.0.0** (same sha256, `47d6d1ff…637b94d`); nothing about the deployed engine changes. This release exists to distribute updated shared scripts through their official channel, the release assets:

- `check_engine_parity.py` gains the `--against-bundle MANIFEST` mode (CHI #91) — the post-assembly assertion the mechanization playbook's migration step 4 and the NYC work order's Phase 1.4 consume block call for. Drift exits 2 under `--strict`; a corrupt artifact (manifest/bundle disagreement, wrong target) is a hard exit-1 FAIL. Forks receive it via their engine-bump PR, which refreshes shared scripts from these assets; after merging the bump, add the two assertion lines to the deploy workflow's assemble job.
- `apply_engine.py` is unchanged from v1.0.0.

This is also the first release to exercise the fan-out → automated bump-PR path end to end (DistrictExplorer-NYC's `engine-bump.yml`, merged in NYC #22).
