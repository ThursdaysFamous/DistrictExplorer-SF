Script-asset release — the engine bundle is **byte-identical to v1.0.0–v1.0.2** (same sha256, `47d6d1ff…637b94d`); nothing about the deployed engine changes. Two script updates ship in the assets:

- `generate_metro_files.py` gains `--sync-fleet` (CHI #97, Conversion 3): refreshes the worksheet's `metro_explorers` from the fleet manifest (`chidistricts.com/metros.json`), rendering only that value so the rest of the worksheet stays byte-stable; mutually exclusive with `--check` so the CI gate stays hermetic. Launching a new metro is now a `--sync-fleet` regeneration PR per fork.
- This release marks the **first reverse-parity back-port** under the ENGINE_SYNC definition of done: the NYC-born `negative-point-ground-truth` validator check (DistrictExplorer-NYC#28) is back-ported to CHI's `validate_index.py` in the same PR that pins this release — clearing the WARN fleet-status issue #98 raised. Validators are fork files ("port checks, not bytes"), so the check travels in the repo, not the assets; this release is its done-marker.
- `apply_engine.py`, `check_engine_parity.py`, and `metro-worksheet.schema.json` are unchanged.
