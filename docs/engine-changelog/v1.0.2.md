Script-asset release — the engine bundle is **byte-identical to v1.0.0 and v1.0.1** (same sha256, `47d6d1ff…637b94d`); nothing about the deployed engine changes. This release distributes the Conversion 2 toolchain through the shared scripts' official channel, the release assets:

- `generate_metro_files.py` (new asset) — the per-fork docs/config generator (CHI #94): emits every GENERATED region from `metro-worksheet.json`, with the `--check` CI drift gate. Forks place it at `scripts/generate_metro_files.py`.
- `metro-worksheet.schema.json` (new asset) — the worksheet's JSON Schema; forks place it at `schema/metro-worksheet.schema.json`. Requires the `jsonschema` pin from `scripts/requirements.txt`.
- `apply_engine.py` and `check_engine_parity.py` are unchanged from v1.0.1.

This unblocks the NYC work order's Phase 2 prerequisite ("CHI publishes `generate_metro_files.py` + `schema/metro-worksheet.schema.json` in the engine release"). NYC bootstraps the two new assets by hand once, per its Phase 1.2 precedent, and extends its bump workflow to refresh them thereafter.
